# NEBULA-QUANT v1 | nq_strategy rules — breakout

from typing import Any


def rule_breakout_above(bar: Any, level: float) -> bool:
    """Stub: true if bar closes above level. Reusable rule."""
    # Skeleton: no real logic
    return False
