# NEBULA-QUANT v1 | nq_strategy rules — trend

from typing import Any


def rule_trend_up(bar: Any) -> bool:
    """Stub: true if bar suggests uptrend. Reusable rule."""
    # Skeleton: no real logic
    return False
