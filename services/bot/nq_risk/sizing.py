# NEBULA-QUANT v1 | nq_risk sizing (stub)

from typing import Any


def compute_size(signal: Any, context: dict[str, Any]) -> float:
    """
    Stub: compute position size from signal and context.
    Returns a placeholder (e.g. 1.0 = 1 unit/contract).
    No broker or portfolio integration yet.
    """
    _ = signal
    _ = context
    return 1.0
