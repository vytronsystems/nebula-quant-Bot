# Binance Testnet API only (read-only; no bot)
FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir fastapi "uvicorn[standard]" requests
COPY . .
ENV PYTHONPATH=/app
EXPOSE 8082
CMD ["uvicorn", "binance_api:app", "--host", "0.0.0.0", "--port", "8082"]
