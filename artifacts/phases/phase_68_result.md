# Phase 68 — Operator Cockpit — Result

## Objective
Build the operator screens.

## Completed tasks

1. **Venues Overview** — `/operator/venues` (placeholder; backend: dashboard aggregation contract).
2. **Instruments Control** — `/operator/instruments` (placeholder; contract: instrument_control_ui.md).
3. **Strategies Registry** — `/operator/strategies` (placeholder).
4. **Paper Trading Monitor** — `/operator/paper` (placeholder; paper_trading_daily_snapshot / nq_paper).
5. **Live Trading Control** — `/operator/live` (placeholder; MFA/approval; control plane live APIs).
6. **Orders & Positions** — `/operator/orders` (placeholder; reconciliation + venue snapshots).
7. **Risk Center** — `/operator/risk` (placeholder; nq_risk / guardrails).
8. **Audit & Evidence Center** — `/operator/audit` (placeholder; artifact_registry / GET /api/artifacts).

Operator home `/operator` updated with links to all eight screens. Layout shell and LanguageProvider in place.
