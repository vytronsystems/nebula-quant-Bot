# Phase 60 — Evidence Backbone

## Objective
Implement DB-first and filesystem-second evidence tracking.

## Tasks
- add DB schema/tables for phase/test/audit/artifact tracking
- create `artifacts/` structure
- add helpers to write manifests and HTML reports
- ensure each future phase can register itself in DB and filesystem
