global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 3h

receivers:
- name: default
  # placeholder: add webhook_url or other config when alert routing is needed
