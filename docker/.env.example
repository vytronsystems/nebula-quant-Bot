# Docker stack — copy to docker/.env or set in parent .env for compose.
# Used by: docker compose -f docker/docker-compose.yml

POSTGRES_USER=nebula
POSTGRES_PASSWORD=nebula123
POSTGRES_DB=trading
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
