import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function OrdersPositionsPage() {
  return (
    <Card>
      <CardHeader><CardTitle>Orders &amp; Positions</CardTitle></CardHeader>
      <CardContent><p className="text-sm text-gray-400">Placeholder — reconciliation + venue snapshots.</p></CardContent>
    </Card>
  );
}
