import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function AuditEvidenceCenterPage() {
  return (
    <Card>
      <CardHeader><CardTitle>Audit &amp; Evidence Center</CardTitle></CardHeader>
      <CardContent><p className="text-sm text-gray-400">Placeholder — artifact_registry / phase_execution_log / GET /api/artifacts.</p></CardContent>
    </Card>
  );
}
