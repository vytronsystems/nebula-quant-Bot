import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function StrategyHealthPage() {
  return (
    <Card>
      <CardHeader><CardTitle>Strategy Health Cards</CardTitle></CardHeader>
      <CardContent><p className="text-sm text-gray-400">Placeholder — strategy registry / promotion.</p></CardContent>
    </Card>
  );
}
